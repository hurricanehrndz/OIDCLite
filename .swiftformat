# Adopt swiftformat as a lint gate without reformatting the existing codebase.
# Only the trailing-whitespace rule is enforced (aligns with the pre-commit
# trailing-whitespace hook). Enable more rules over time as the code is cleaned up.
--swiftversion 5.5
--rules trailingSpace
